{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run C Program",
      "type": "cppdbg",
      "request": "launch",
      // aponte pro .exe gerado pela sua task
      "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
      "args": [],
      // faz com que o terminal integrado seja usado
      "console": "integratedTerminal",
      "externalConsole": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "MIMode": "gdb",
      // ajusta o pré-build pra chamar sua task de compilação
      "preLaunchTask": "C/C++: gcc.exe build active file"
    }
  ]
}
